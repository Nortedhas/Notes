<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/activity_authentication"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg"
        tools:context="com.example.notes.Authentication.AuthenticationActivity">

    <EditText
            android:gravity="center_horizontal"
            android:layout_width="300dp"
            android:layout_height="70dp"
            android:maxLines="2"
            android:id="@+id/auth_login_edit_text"
            android:textStyle="bold"
            android:hint="Login"
            android:fontFamily="sans-serif-condensed"
            android:textSize="@dimen/nameSize"
            android:textColor="@color/textColor"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="170dp"
            android:inputType="textEmailAddress"

    />
    <EditText android:layout_width="300dp"
              android:layout_height="50dp"
              android:gravity="center_horizontal"
              android:layout_centerHorizontal="true"
              android:layout_centerVertical="true"
              android:layout_below="@+id/auth_login_edit_text"
              android:textStyle="bold"
              android:hint="Password"
              android:fontFamily="sans-serif-condensed"
              android:textSize="@dimen/nameSize"
              android:textColor="@color/textColor"
              android:id="@+id/auth_password_edit_text"
              android:inputType="textPassword"
    />

    <LinearLayout android:layout_width="match_parent"
                  android:layout_height="wrap_content"
                  android:layout_below="@+id/auth_password_edit_text"
                  android:orientation="vertical"
                  android:layout_marginTop="150dp"
    >
        <Button android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:text="sign in"
                android:layout_gravity="center"
                android:layout_weight="1"
                android:id="@+id/btn_sign_in"
                android:background="@android:color/transparent"
        />

        <Button android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:layout_gravity="center"
                android:layout_weight="1"
                android:layout_marginTop="20dp"
                android:text="Sign up"
                android:id="@+id/btn_sign_up"
                android:background="@android:color/transparent"
        />

    </LinearLayout>
</RelativeLayout>